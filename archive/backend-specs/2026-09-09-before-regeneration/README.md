# Archived backend implementation packets

Archived on 2026-09-09 before fresh spec generation.

These four packets and their original progress tracker are historical records, not active
requirements or implementation instructions. They predate the timed-unit, batch-submission
and marks-only V1 revision. Do not use them as source material or compare against them when
writing replacement packets unless the project owner explicitly requests historical analysis.

Generate replacements from the current PRD, module documents, data model, contracts and actual
repository state, following the installed spec-writing skill. Check newly generated packets
against those authoritative sources and against one another for consistency. If a packet
needs a new interface or product decision, update the authoritative documents explicitly;
do not invent a requirement or import one from this archive.

The source files are preserved byte-for-byte. SHA256SUMS.json records their original hashes.
The archived PROGRESS.md describes the old generation only; active progress is in
../../../todos/PROGRESS.md.
