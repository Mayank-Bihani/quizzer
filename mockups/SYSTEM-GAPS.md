# System gaps

Things a screen needed that the design system does not have. Each entry names the component
and the screen that wanted it, so the design system can be amended rather than worked around.

| # | Needed | Screen | Worked around by |
|---|---|---|---|
| 1 | Web fonts (IBM Plex Sans / Mono, Source Serif 4) | all | **Resolved 4 Sep** — latin woff2 subsets vendored into `assets/fonts/` and declared in `css/fonts.css`. No external requests; mockups render in production type. |
| 2 | `.b-live` and `.b-sec` ship a hardcoded white; `.fab`, `.seg button.on` and `.av` too | all | Replaced with `var(--n-0)` during S0 extraction. Worth fixing upstream in the design system. |
| 3 | No `page-header` back-link component; variant A composes it inline from an anchor | S0 shell | Built as `.phdr__back`. Add to the system if S1 needs it elsewhere. |
| 4 | No nested-surface component — `.card` inside `.card` double-blurs | S0 student home | Added `.panel` (uses `--glass-bg-strong`, no backdrop-filter). |
| 5 | No token for the segmented-control track — the guidelines use a literal `rgba(16,24,40,.05)` translucent wash | S0 `.seg` | Used `--surface-subtle` (opaque `--n-100`). Very close, but opaque where the original was translucent. A `--surface-wash` token would close it. |
| 6 | The admin import screens mock an invented CSV schema (`stem`, `section`, `correct_option`) | S8 | The real schema is [[BANK]] §3. S8 rebuilds the validation copy against it — the guidelines column names must not be copied. |
| 7 | No Google sign-in button, and no tokens for the Google brand colours | S1 sign-in | `.btn.sec` + the Google mark as inline SVG in `currentColor`. Google's guidelines want the four-colour mark; the mockups have no palette for it and may not fetch one. The real app should ship the official asset from `assets/`. |
| 8 | `.btn.sm`, `.chip` and `.seg button` are 36px — below the 44px touch-target rule the brief sets for every width | S0 components, hit in S1 | S1 uses full-size `.btn` for anything a student taps on a phone (Copy code, Share, Back to Home). `.btn.sm` survives for dense admin rows where a mouse is assumed. Worth a decision upstream: either raise the small size to 44px or state where 36px is allowed. |
| 9 | No token for muted text on an inverse surface — the guidelines' `.codebox .tiny` uses a literal `rgba(255,255,255,.6)` | S1 quiz detail (`room-code-display` A) | Used `var(--n-400)` on `--surface-inverse`. Close, and it passes contrast, but a `--text-inverse-2` token would make it exact and reusable by S9's admin room-code block. |
| 10 | No account-menu component in `components.css`; the guidelines carry it as loose markup under `avatar-name-row` | S1 profile | Extracted as `.menu` into `components.css` §24. It is a real component with a `<hr>` separator and a danger item, not page furniture. |
| 11 | `join-room-code` A needs a hidden input behind the eight slots — the guidelines note this but ship no markup for it | S1 join by code | Built as `.codefield` (a real `<input>` at `opacity:0` over the slots, `.codeslots` marked `aria-hidden`), with the focus ring moved to `.codefield:focus-within`. Paste, autofill, caret and screen readers all work; the slots are decoration. |
| 12 | The guidelines' `.slot` is a fixed 42px, and eight of them plus a hyphen overflow 390px inside a card | S1 join by code | Made the slot fluid (`flex:1 1 0; max-width:42px`), so it shrinks to ~37px at 390 and reaches 42px from ~430 up. Height stays `--tap-comfortable`. |
| 13 | Every lobby and detail sample in the guidelines hardcodes `+3 / −1 / +0.5` in copy | S1 lobby, quiz detail | Per `selections.md` consequence 9, marks are quiz data. S1 renders three different schemes — Quant `+3/−1/+0.5`, Verbal `+2/0/+0.4`, and the explicit "no negative marking" strip from consequence 10 — so no screen can be copied with the numbers baked in. |
| 14 | **No skip control anywhere in the design system.** ROOM-17 and [[JOURNEYS]] §SJ-4 require it, `MOCKUP_PROMPT` makes it an acceptance item, and Batch 3 of the guidelines ships `submit-lock-action` with three variants and no second action of any kind | S2 all three run screens | Built in `components.css` §33 as `.qact` / `.qact__skip` / `.qact__note`: a `.btn.sec` in its own row above Submit & lock, with the consequence line ("Scores 0. You cannot come back to it.") physically occupying the gap between the two. That gap is the whole mitigation for a mis-tap, so it is documented as load-bearing. The design system should adopt a `skip-action` component with variants, because this was designed here rather than chosen. |
| 15 | `submit-lock-action` A's lock confirmation is specified in `--success-50/500/700` | S2 answer locked (MCQ and TITA) | Built neutral (`--surface-subtle` / `--border-strong-c` / `--text-2`). Green on the one screen where correctness may never appear is a misreadable signal — a student seeing a green ring the instant they lock will read it as "right", and it is shown identically for every answer. The padlock plus the words "Answer locked" carry the state instead. Worth resolving upstream: either the run gets an explicitly non-semantic confirmation colour, or the token set gains a neutral "committed" pair. |
| 16 | `question-timer` B's grace mark is drawn under the fill in the guidelines (`.grace`, `.gmark`, then `<i>`), so the grace boundary is invisible for the first three-quarters of every question — exactly when it matters | S2 all three run screens | `.tbar__mark` given `z-index:1` and cut in `--n-0`, so it reads as a notch over the grace fill, the normal fill and the bare track alike. The guidelines also let the mark overhang the track (`top:-3px;bottom:-3px`), which `overflow:hidden` on the rounded track makes impossible; the notch replaces that. |
| 17 | No size token between `--sp-9` (32px) and `--tap-min` (44px), and none for bar heights | S2 timer bar, progress bar, option letter chip | Bars are `--sp-3` (progress, 6px), `--sp-5` (question timer, 12px) and `--sp-6` (final seconds, 16px) instead of the guidelines' 6/10/12; the letter chip is `--sp-9` instead of 26px. Every value is a token and the 3-step hierarchy the component needs survives, but spacing tokens are being used as sizes. A small `--size-*` scale would close it — the same gap `.av` (36px) and `.slot` (42px) hit in S0 and S1. |
| 18 | The S1 handover recorded that `tita-answer-run` A renders the answer in `.codeslots`. It does not — that is `tita-numeric-input` **C**, a losing variant | S2 TITA | A is a single big display over the keypad, and the picked `tita-numeric-input` B agrees. Built as `.answerbox` (a real `<input>`, so it carries the label, paste, autofill and `inputmode="decimal"`) plus `.keypad`. `.codeslots` was not touched and stays the room-code component. |
| 19 | **No rotate-to-portrait overlay component anywhere in the design system.** MOCKUP_PROMPT's "Orientation" section requires it (Screen Orientation Lock API needs fullscreen and is unsupported on iOS Safari) but none of the six guidelines artifacts model it — Batch 3 has thirteen live-run components and this is not one of them | S3 rotate overlay | Built from scratch in `components.css` §39 as `.rotate-lock`: `display:none` until `@media (orientation:landscape) and (max-width:899px)`, then a fixed full-viewport cover on `--surface-inverse`, above the two clocks (`z-index:60`). `.rotate-lock--demo` forces the identical markup visible unconditionally, because a portrait screenshot at 390/820/1440 can never show the real trigger firing — the proof page and `run-rotate.html` both say so explicitly rather than pretending the screenshot is the real behaviour. The design system should adopt this component so later phone-locked flows (if any) don't re-derive it. |
| 20 | **`inline-alert` C (`.strip`) ships no markup for the reconnecting/offline/reconnected trio as a real component** — the guidelines draw ad hoc `.strip` divs inline with `style=` attributes wherever a connection state is needed, never as a documented component with states | S3 reconnecting | Extracted into `components.css` §38 as `.strip` + `.strip .dot` (the pulsing connectivity dot), reusing the same `.a-info`/`.a-warn`/`.a-dgr`/`.a-ok` status-colour classes `.alert` (§15) already defines, so the palette is one decision shared by both surfaces rather than two. Confirms the S0/S1/S2 handover notes that flagged this as still-unbuilt. |
| 21 | **No stated rule that late-join / resumed-after-disconnect numbers are per-session data**, the way `selections.md` consequence 9 states it for the marks scheme — every guideline sample hardcodes them in prose (`14 minutes`, `18 of 24 questions`) with nothing marking them as computed | S3 late-join, resumed | Every number in `run-interrupt.html` is wrapped in `<span class="num">` so it at least reads as data, matching the run's existing convention, but there is no written rule against copying the sample numbers verbatim the way consequence 9 exists for marks. Worth adding one line to `selections.md` alongside it. |
| 22 | **`rank-reveal` B's neighbour rows (`.lb`) are ad hoc markup with inline `style="grid-template-columns:..."` overrides** — not a documented component, and not the same shape as the separate `leaderboard-row` A pick (Batch 4) that S6 will build for the full per-quiz board | S4 rank reveal | Built as `.rankrow` (`components.css` §42), which deliberately reuses S1's `avatar-name-row A` (`.namerow`, `.namerow.you`) rather than inventing a second avatar-row shape — a rank number is prepended and a score appended as plain markup, no new avatar or name styling. **S6 should decide whether `leaderboard-row A` extends `.rankrow`/`.namerow` the same way, or replaces it** — building the two independently risks two visually-similar-but-different avatar rows in the same product. |
| 23 | **No `score-breakdown-tiles` ledger component exists anywhere** — the guideline's reconciling-ledger variant (B) is drawn from `.qc` (a run-question-card class, repurposed) plus ad hoc `.tilerow` divs with inline background overrides for the total row, not a named, reusable list | S4 score breakdown | Built as `.ledger` / `.ledger__row` / `.ledger__row--total` / `.ledger__val` (`components.css` §43). This is the **first component in the whole build allowed to colour text with `--rv-correct-fg` / `--rv-wrong-fg`** outside the review screen itself — legitimate per the disclosure-ladder rule (tier 3, post-deadline only) but worth flagging because `review-question-card` (S5) will want the identical two colours and should reuse `.ledger__ok` / `.ledger__no` rather than rederiving them. |
| 24 | **No shared "big number, small caption above it" typography pattern**, despite three Batch 4 components needing exactly that shape (finish-card's score, rank-reveal's rank, holding-state's score recap) — the guidelines redraw it from scratch with inline styles (`.hero`, `.bignum`, `.wlabel`) in every section rather than naming it once | S4 finish, holding, rank reveal | Built as shared typography helpers in `components.css` §41: `.wlabel` (the neutral overline caption, distinct from `.eyebrow`'s accent colour), `.hero` (fs-64, for inline placement next to other text, e.g. rank-reveal's "4 · of 83"), and `.numhero`/`.numhero__value` (a stacked label+value block, with a `--lg` modifier). All three sprint screens now draw from the same three classes instead of three more one-off inline styles. |
| 25 | **Only four review outcomes are modelled, not five.** JOURNEYS §SJ-7 and MOCKUP_PROMPT both list correct / wrong / skipped / **timed out** / not reached as distinct outcomes to render, but the guidelines' `review-question-card` and `review-navigator` sections only ever draw four badges (`b-ok`/`b-no`/`b-skip`/`b-nr`) and four cell colours — their own sample review card uses the **skip** badge for a question where "time expired before you submitted", which is exactly the case JOURNEYS separately names "timed out". There is also no fifth `--rv-timedout-*` token family. Separately, `review-navigator` A's pick is a **sticky bottom bar**, which the guidelines never reconcile with `app-shell-student` A's own persistent bottom tab bar — nothing in Batch 2b or Batch 4 says what happens when both want the same edge on a phone | S5 review | Added `.b-timeout` / `.rcell.timeout` (`components.css` §45, §49), reusing `--rv-skipped-*` — legitimate because both outcomes score 0 with no penalty (SCORE-4) — but with a dashed border and its own glyph/word (⌛ / "Timed out") so the two are never visually merged, per the guideline's own rule that outcome is always a glyph + a word + a colour. For the bottom-bar collision: `review.html` **suppresses `.navbar` entirely** and lets `review-navigator` A's sticky bar take that edge, with the page-header back-link (and the shell's `.topnav` from 900px up) as the way back to Home/History/Boards. The design system should either drop the "sticky bottom bar" framing for `review-navigator` A or explicitly state this hand-off in `app-shell-student`'s spec. |
| 26 | **`answer-distribution`'s three variants (mini bars, stacked bar, sentence) all assume four lettered MCQ options** — none account for a TITA / numeric-answer question, which has no options to bar-chart, even though RESULT-5 draws no MCQ/TITA distinction ("what percentage chose each option") | S5 review, component-reference state | Reused `.dist`/`.dist__row` with a wider first column (bucket labels — "Within ±1 of the correct value", "Off by more than 1", "Skipped" — instead of a single letter) rather than inventing a new component, shown as a labelled reference alongside the two MCQ edge cases. The design system should add a fourth, TITA-specific variant rather than leaving it to be improvised per sprint. |
| 27 | **`review-question-card`'s pick is "C + A composed"** (selections.md consequence 8: "C is the index, A is the expanded detail") **but the guidelines never show the composition as one interaction** — variant C's own trio entry expands a *different*, abbreviated card shape inline, not variant A's full annotated-option-list-plus-explanation card, so "opens into A" is asserted but never actually demonstrated | S5 review, collapsed-index state | Built literally: a list of `.rrow` buttons (C) where one row (`aria-expanded="true"`) sits directly above a real `.rcard` (A) rendered inline, sharing the same option-annotation markup as the full-detail states elsewhere in the file — not a shrunken stand-in. Confirms the composition is buildable as specified; the design system's own artifact should show it this way too. |
| 28 | **RESOLVED — `.rankrow` vs. `leaderboard-row A` (see #22).** The two were flagged as visually-similar-but-different avatar rows built independently; S6 had to pick one path before building the per-quiz and weekly boards | S6 leaderboard-quiz, leaderboard-weekly | Extended `.rankrow` (S4 §42) rather than inventing a second component. New pieces are additive and opt-in: `.rankrow--list` (its own surface + border, for a long standalone list — `rank-reveal.html`'s short in-card context list never gets this modifier and is pixel-unchanged), `.r1`/`.r2`/`.r3` medal classes, `.rankrow__tb`/`__sec`/`__count` (hidden below 900px, the sprint's one relayout), and a shared `.badge.b-you` tag. `.rcell`/`.rgrid` (S5) were correctly left alone, per the S5 handover — a leaderboard row (one row per other student) is a different axis from a review cell (one cell per this student's own question). |
| 29 | No token exists for a 3px "medal rule" border width — `--border-hairline` (1px), `--border-strong` (1.5px) and `--border-focus` (2px) are the full set | S6 leaderboard rows, rank 1–3 | Used `--border-focus` (2px), the widest available. Close but not exact; a `--border-thick` (~3px) token would close it. Same family of gap as #17 (no `--size-*` scale) — border widths have the identical problem spacing tokens do. |
| 30 | **No `pagination` component was built** (still unbuilt per the S0 "deliberately not built yet" list) — History's "load more" and both leaderboards' 80+ row lists all need it in the real app | S6 history, leaderboard-quiz, leaderboard-weekly | History uses a plain `.btn.sec` labelled "Load earlier attempts" with no pagination markup behind it. The two leaderboards render a representative truncated list (top ranks, the student's own neighbourhood, the bottom ranks) with a plain-text "⋯ N more ⋯" divider between chunks, plus a static `#`-anchor "Jump to my rank" link — real, working scroll-to, but not the sticky mini-row the leaderboard-row A guideline describes ("pins your position to the bottom edge when you scroll past it"), which needs scroll-position JS the static-markup rule (constraint 3) doesn't allow a mockup to fake convincingly. Both are workarounds for the still-missing `pagination A`; the real app should build it rather than ship either truncation approach. |
| 31 | **BOARD-6 says weekly boards are "per section type and overall" but never specifies what a per-row "section" column means on the *overall* board**, where a student's average is computed across quizzes that may span multiple sections | S6 leaderboard-weekly, Overall tab | Rendered `.rankrow__sec` on the Overall tab as the student's most-attempted section that week (a judgement call, stated nowhere in BANK/PRD/JOURNEYS) and omitted it entirely on the per-section tabs (redundant once already filtered to one section) and on the per-quiz board (a single quiz has one section, already in the page header). Worth a real product decision — the alternative is that "Overall" simply has no section column at all. |
| 32 | **S0's admin shell has no mobile story at all.** `app-shell-admin` A ships `.adm__side{display:none}` below 900px and nothing to replace it — no drawer, no trigger, no hamburger — even though MOCKUP_PROMPT's reflow/relayout table explicitly requires "Admin navigation: Drawer (rare, but must not break)". The design system's own layout-feedback.html even says so out loud in its Batch 2b "Open questions": *"Admin on mobile. Assumed the console is desktop-only… Confirm."* — an assumption MOCKUP_PROMPT's own table overrides | S7 all four admin screens | Built `.admdrawer` (`components.css` §56): a real, zero-JS `<details>/<summary>` disclosure — no click handler needed, natively keyboard-operable — reusing `.menu` (§24) for the link list rather than inventing a second dropdown shape. Hidden entirely at ≥900px, where `.adm__side` takes over; the two never overlap. The design system should adopt this pattern (or a considered alternative) rather than leaving admin genuinely unusable below 900px. |
| 33 | **`data-table` A's own trio note admits dense rows are 36px** ("trades: most rows per screen; tiring for long sessions") but neither that entry nor `question-row` A/C ever states how the two combine below 640px — building both required inferring that the *whole table* (not just individual rows) swaps for the card list, on the same min-width-only technique the student shell's navbar/topnav already use | S7 bank.html | Built as one shared toggle in `components.css` §57 (`.tablewrap{display:none}` → `display:block` at `≥640px`), so any future `data-table` screen (S10's participants-table, which picked the same A) inherits the swap for free instead of re-deriving it. |
| 34 | **Grid/flex blowout is a recurring, unfixed class of bug, not a one-off.** S1 already hit and fixed it once (`.profile > *{min-width:0}`, "Fix after S1 review"), and this sprint hit it twice more independently: `.tablewrap` (a grid item of `.bankhead`) grew to its table's min-content width instead of scrolling internally, and `.pgroup__head` (itself `display:grid`) grew to accommodate a nowrap `.rowflex-between` title row, both because grid/flex items default `min-width` to `auto` (= their own min-content) rather than `0`. Neither the design system nor `MOCKUP_PROMPT` documents this as a standing risk, so every sprint has been rediscovering it by measurement rather than by rule | S7 bank.html (`.tablewrap`), passage-group.html (`.pgroup__head`) | Fixed at the source: `.tablewrap{min-width:0}` (§57, benefits every future table) and a new `.pgroup__toprow`/`.pgroup__title` pair with explicit `min-width:0` at every level (§62), replacing a reused `.rowflex-between`+`.rowflex` nesting that had never been exercised with a long title before. **Worth a standing rule**: any `display:grid` or nowrap `display:flex` container holding text content should default to `min-width:0` on its children, the same way `.grow` already does — this should be stated once in the S0 contract rather than re-found by each sprint's own measurement pass. |
| 35 | **No token exists between `--tap-min` (44px) and the next size down** for exactly the case a native `<input type="checkbox">` needs — a control that is visually small (~16–18px) but must still clear a 44px tap area on a touch surface | S7 bank.html, mobile card fallback | The dense desktop table's checkbox stays native-sized — an accepted trade already documented by `data-table` A's own "36px rows" note, and desktop is mouse-driven. The mobile card fallback exists specifically *because* a touch device is plausible there, so its checkbox is wrapped in a new `.bankcard__check` label (`components.css` §58) sized to `--tap-min` with the native control centred inside — measured at 44×44px. No component in the design system addresses checkbox tap sizing at all; every guideline sample uses a bare `<input type="checkbox">` at native size regardless of context. |
| 36 | **`question-editor` A's own trade note calls for the two-pane split at "≥1000px"**, which is not one of this build's three breakpoints (640/900/1280) | S7 item-editor.html | Substituted `min-width:1280px` (the nearest breakpoint at or above 1000) for the split; it stacks (edit above, preview below) at every width below that, which is not a second variant, just the same component reflowing — `question-editor` is not one of MOCKUP_PROMPT's eight genuine-relayout screens. Worth adding 1000px to the breakpoint set, or restating the guideline's own number as 1280, so a later sprint doesn't have to make the same substitution independently. |
| 37 | **`passage-group-card` A's own sample (admin.html) never actually renders at a phone width** — like the rest of Batch 5, it is drawn once at 1280×760 with no responsive check at all, so nothing in the guideline says what happens to a 198-word passage, four question rows and a header badge row when the viewport is 390px wide | S7 passage-group.html | Verified by measurement (not assumed): the header wraps to two lines below its available width via `.pgroup__toprow{flex-wrap:wrap}` rather than truncating the group title, and each child row's stem wraps (`overflow-wrap:anywhere`) rather than the guideline's implied one-line-with-ellipsis treatment — a passage group's title and question stems are exactly the content an admin needs to read in full to catch a mis-titled or mis-worded row, so eliding them for width would defeat the screen's purpose. |
| 38 | **No size token exists for a handful of one-off admin measurements** — the dense table's `min-width:560px` (below which columns become unreadable rather than merely tight), the mobile drawer menu's `min-width:200px`, and the two-pane editor's preview column `minmax(320px,380px)` — none derivable from `--sp-*`, `--fs-*` or any existing scale | S7 bank.html, item-editor.html, admin drawer | Same family of gap as #17 (spacing tokens standing in for a missing `--size-*` scale) and #29 (missing border-width token) — not fixed here, just added to the running tally so a future token pass fixes all three at once rather than one sprint at a time. |
| 39 | **RESOLVED — #6 (invented CSV schema) closed by S8.** The design-system guideline's `csv-validation-preview`/`import-error-summary`/`import-commit-bar` sections all mock a schema (`stem, section, correct_option`) that does not match the product's real one | S8 all three screens | Every error message, column name and stat in this sprint quotes `[[BANK]] §3`'s real header row (`type, topic, subtopic, difficulty, format, passage_ref, body, image, option_a..d, correct, numeric_answer, tolerance, explanation, source`) and validates against its twelve rules in §3.1. The guideline's invented header appears exactly once, deliberately, as the rejected legacy file in `import-preview.html`'s "nothing importable" state — the wrong answer being turned away, not copied. |
| 40 | **`csv-validation-preview` and `import-error-summary` are catalogued as competing variants of adjacent-but-separate components, never composed on one screen** — the guideline's own trio for each is drawn in isolation, so nothing demonstrates what the picked pairing (validation B + error-summary A, both locked in `selections.md`) actually looks like assembled | S8 `import-preview.html`, mixed state | Built as a real composition: an `.alert.a-dgr` banner, then the grouped-by-cause panels (`import-error-summary` A, six one-line causes each naming its column and CSV line number), then a zero-JS `<details>` disclosure revealing a sample of the collapsed valid rows (`csv-validation-preview` B's "valid collapsed" half). Confirms the pairing is buildable as specified, the same kind of gap #27 logged for `review-question-card` C+A. |
| 41 | **`import-commit-bar` A's guideline sample says only "Committing…" during the write** — it never engages with BANK.md §3.3's actual mechanism (chunked `batch()` writes keyed to one `import_id`, a full `DELETE … WHERE import_id = ?` rollback on any chunk failure), so nothing in the design system states how atomicity should read on screen | S8 `import-commit.html`, committing &amp; failed states | Added explicit chunk-progress copy ("Writing in chunks of 50 rows · 142 / 194") to the committing state, and a dedicated `.commitbar--err` tone plus an `.alert.a-info` explainer on the failed state that states the all-or-nothing guarantee in plain language ("the bank never holds a half-finished import") — never "partially imported," which the guideline's own three-state commit bar doesn't rule out on its own. |
| 42 | **`file-dropzone` A's own states (idle/drag/uploading/error) are drawn with exactly one file at a time** — no guideline sample shows the CSV and its companion ZIP (BANK-1, BANK-4) present together and progressing independently, even though variant A is explicitly "single zone, two file types" | S8 `import-upload.html`, uploading &amp; upload-failed states | Built with two independent `.filerow`s inside one `.dz--up`/`.dz--err` block — the CSV finishes first (badge `.b-open`, "✓ Uploaded") while the ZIP is still transferring (`.pbar` fill + percentage), and the error state shows the CSV already uploaded alongside a ZIP that failed on size — demonstrating the two-files-one-zone model the guideline names but never draws. |
| 43 | **`modal-sheet` and `destructive-confirm` were both deferred since S0's "not built yet" list, and neither guideline artifact ever models the real production behaviour** — `admin.html`/`layout-feedback.html` only ever show `.scrim`/`.dlg`/`.sheet` boxed inside a catalogue `.stage` (a bounded demo container with `position:relative`), never as a true viewport-covering overlay. Building the real fixed-to-viewport version was tried first and broken: `position:fixed` dialogs from two stacked states in one file both try to cover the same viewport at once, an unreadable collision the static-file "stack every state in one document" convention (MOCKUP_PROMPT "one screen per file... render states stacked") never anticipated for an overlay component | S9 `builder-lock.html` | Matched the guideline's own choice instead of inventing portal behaviour: `.scrim`/`.dlg`/`.sheet` are `position:absolute`, scoped to a positioned ancestor the screen provides (`.stage` in `screens-s9.css`, the same shape as the guideline's `.stage`). A real app renders these into an actual viewport-covering portal — ordinary implementation detail, not a variant choice, and not modelled here because nothing in the design system models it either. `.dlg--demo`/`.sheet--demo` (forcing one shell visible regardless of width) reuse the exact "--demo" technique S3's `.rotate-lock--demo` (§39) established for a component whose real trigger a fixed-width screenshot can't show — though `builder-lock.html` didn't end up needing them, since its normal min-width toggle is directly visible in the 390 vs. 820/1440 proof frames. |
| 44 | **`room-code-display` A is not shown on the schedule step, on purpose — not a gap in the strict sense, but worth recording since a future sprint could plausibly "complete" the schedule step by adding it.** QUIZ-8 assigns the room code at lock, not at scheduling, so no code exists yet while an admin is still picking a date | S9 `builder-schedule.html` | No component shown at all for the code on this step — the "no stub" precedent (S2's grace band, S6's per-quiz section cell) extended to "no component at all" rather than a placeholder pending-state box. `room-code-display` A's actual pending state (`.codebox--pending`, S1 §20) still correctly belongs to the student-facing quiz-detail/lobby screens, which show a countdown to release, not to the admin builder. |
| 45 | **PRD open question 1 (pool exhaustion: "fail loudly, draw fewer, or reuse with a warning?") is answered here as a judgement call, not a resolved product decision** — JOURNEYS explicitly leaves it open and says only that "whichever, it surfaces here [the draw step]" | S9 `builder-draw.html`, pool-exhaustion bonus state | Resolved as reuse-with-an-explicit-flag: the two slots a shortfall can't fill from the never-used pool are filled with previously-used questions, individually badged `Reused` in the draw list, with a warning alert stating the shortfall count and that reused rows are still retired at lock like any other drawn question. This is the direction `admin.html`'s own `difficulty-mix-control` A note already leans ("asking for more will reuse questions"), extended into an explicit UI treatment. A real product decision should confirm or override this before implementation. |
| 46 | **QUIZ-10's window-length rule ("long enough to reach every question at the per-question limit, plus slack") has no corresponding validation in any guideline variant** — `schedule-picker` B models only "close time is before open time," a different and much narrower check | S9 `builder-schedule.html`, window-too-short state | Built directly from QUIZ-10's own wording: a window shorter than `count × time_per_q` is rejected with the specific arithmetic named in the error ("20 questions × 45s need at least 15 minutes, plus slack"), Continue disabled. The design system should adopt this as a fourth `schedule-picker` state (today it only shows the derived close time and the open/close-order error) rather than leaving it sprint-specific. |
| 47 | **No token exists for a chart's own light literals** — `scoring-parameter-panel` A's guideline sample hardcodes `fill="#fff"` and `fill="#414A57"`/`"#0C8341"`/`"#7A8699"` directly as SVG presentation attributes, none of which have an exact token equivalent for use inside `<svg>` | S9 `builder-scoring.html`, bonus-decay curve | Every fill/stroke on the curve uses `var(--token)` presentation attributes instead (`var(--surface-raised)`, `var(--text-2)`, `var(--text-3)`, `var(--success-700)`, `var(--accent-500/600)`, `var(--border-1/2)`) — confirmed these resolve correctly for inline SVG sharing the host document's cascade, unlike an external `.svg` file. No literal colour survives in the chart; `check.sh`'s scan (which also covers HTML, not just CSS) confirmed this by initially catching decimal-numeric-entity glyphs (`&#8635;` etc.) that happened to read as valid hex strings — real HTML entities are fine, but ones whose digits are all `0-9a-f` need to be written as literal UTF-8 characters instead, or the colour scanner false-positives on them. Recorded here since it will recur the moment a future sprint pastes a numeric entity from a guideline sample. |
| 48 | **`.seg button`'s 36px height (SYSTEM-GAPS #8, standing since S0) reached a screen for the first time where it is a primary, always-tappable control** — every earlier consumer of `.seg` (student section tabs, the S6 board switcher) either sits above 390px-only content or was already covered by #8's "student-facing" carve-out; `builder-schedule.html`'s one-off/recurring toggle is the step's main decision, present at all three tested widths, on an admin surface `MOCKUP_PROMPT` rule 2 explicitly says may be touched | S9 `builder-schedule.html`, one-off/recurring toggle | Fixed locally with `style="min-height:var(--tap-min)"` on the four affected `<button>`s rather than editing the shared `.seg` rule — changing `.seg` globally would ripple into every earlier sprint's screens and require re-verifying all of them, out of scope here. `.seg`'s base 36px height is unchanged for every other consumer. #8 still stands as the real upstream fix: either raise `.seg button`'s floor to 44px everywhere, or document 36px as mouse-only and audit every existing `.seg` usage against that rule. |
| 49 | **PRD open question 7 (item analysis: "surface it at all?") is resolved here as a mockup judgement call, not a product decision** — JOURNEYS marks AJ-8's item analysis explicitly optional, and PRD lists it under "needs a decision before Phase 4–5," yet `selections.md` already picked a variant (`item-analysis-row` A) for it, which only makes sense if it ships | S10 `report.html`, item analysis section | Built per the locked pick (A — row with % correct bar, sorted worst first) since a variant was already chosen; worded the section intro to make the trade explicit ("useful for catching a bad question, not for judging a student") so the screen doesn't read as an accusation-by-question-number. A real product decision should confirm this ships before implementation — the mockup cannot resolve a PRD open question on its own authority. |
| 50 | **Live-quiz-monitor and quiz-void are absent from every `quiz-card-schedule` A card here, unlike the design system's own sample** — `admin.html`'s guideline draws "Monitor" on the open/live cards and "Void quiz" on the live card as part of the picked variant's own markup, but AJ-7 (PRD open question 5) and AJ-9 (PRD open question 6) are both explicitly undecided, and JOURNEYS says AJ-7 must be "decided before mockups." S10's own scope (schedule list, templates, report, participants, export, manage admins, promote/demote) never names either | S10 `schedule.html`, open &amp; live cards | Both actions omitted outright rather than stubbed, disabled, or linked to a placeholder — building even an inert affordance for an undecided feature would silently resolve the open question in the direction "yes, this ships," which is not this sprint's call to make. The live-quiz-monitor guideline section is itself marked `speculative` for exactly this reason. If either question is answered before implementation, `quiz-card-schedule`'s open/live states are the ones to revisit. |
| 51 | **`report-summary-tiles` B's own guideline sample draws only two tiles beside the histogram** (participants, avg score) — dropping the two other headline numbers (avg time, avg accuracy) that variant A's four-tile layout would have shown on their own | S10 `report.html`, summary section | Built with all four tiles plus the histogram — nothing in `selections.md`'s pick or its consequences caps the tile count at two, and a report missing avg time/accuracy would be a strict regression from A. `.tiles`/`.tile` (components.css §74) is written to hold any count, 2 or 4, so this is a content choice, not a component constraint. |
| 52 | **Grid blowout (SYSTEM-GAPS #34, already hit independently in S1/S7×2/S8) recurred a fifth time** — `.rolegroup` (`display:grid`, single implicit column) let its own `.rowflex-between` children default to their max-content width, which (with an untruncated email string as an unbreakable run) widened the shared grid track enough to push the page header's own row 76px past the 390px viewport, even though the header itself was untouched | S10 `manage-admins.html`, role rows | Fixed at the source: `.rolegroup>*{min-width:0}` and `.rolegroup .stack>*{min-width:0}` (components.css/screens-s10.css), plus `.truncate` added to two `.namerow__meta` lines that were carrying a bare email with no other break opportunity. Measured clean afterward at 390/820/1440 via a scripted Chrome DevTools Protocol pass (not eyeballing) — this makes five independent discoveries of the identical root cause across four sprints with no shared fix; still worth stating once in the S0 contract as MOCKUP_PROMPT itself does not mention it. |
| 53 | **`.seg button`'s 36px height (SYSTEM-GAPS #8/#48) reached a third screen** — `schedule.html`'s Quizzes/Templates toggle is, like S9's one-off/recurring toggle, a primary always-visible navigation control, not a secondary desktop-only one | S10 `schedule.html`, both `.seg` instances | Same local fix as #48: `style="min-height:var(--tap-min)"` on all four affected buttons, `.seg`'s shared rule left unchanged. Three independent local fixes for the same 8px shortfall is itself the strongest argument yet for closing #8 upstream rather than per-screen. |
| 54 | **`manage-admins-row` B's guideline sample never distinguishes what an ordinary admin sees here from what a superadmin sees** — it draws one undifferentiated roster with live role-change controls, but AUTH-5/ADMIN-3 restrict promote/demote to superadmins only, and the signed-in identity on every other admin screen in this build (Rohan Sethi) is an ordinary admin, not a superadmin | S10 `manage-admins.html` | Resolved as a mockup judgement call: any admin can view the roster (read-only, role-change controls present but `disabled` with a stated reason), and a second, separately-labelled state switches the signed-in identity to Priya Menon (superadmin) to demonstrate the live promote/demote controls — the same "switch identity for a stacked state that needs a different session" technique S1's `profile.html` used for its student/admin states. Not a product decision (whether a plain admin may even *view* the roster is genuinely unstated in PRD/JOURNEYS), just the least-contradictory reading available. |
